import React from 'react';
import {
  Dialog,
  DialogTitle,
  DialogContent,
  Button,
  ButtonGroup,
  Container,
  TextField
} from '@mui/material';
import Modal from '../modal/Modal';

export default function AddSceneModal({ devices, rooms, onScene, onSubmit }) {
  return (
    <Modal>
      <TextField />
      {/*// todo: use SceneComposer*/}
    </Modal>
  );
}
