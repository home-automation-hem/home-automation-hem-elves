import React from 'react';
import {
  Dialog,
  DialogTitle,
  DialogContent,
  Button,
  ButtonGroup,
  Container,
  TextField
} from '@mui/material';
import Modal from '../modal/Modal';

export default function AddRoomModal({ devices, rooms, onScene, onSubmit }) {
  return (
    <Modal>
      <TextField />
    </Modal>
  );
}
